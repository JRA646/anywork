VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Supabase Edge Functions / transactional email
RESEND_API_KEY=
ANYWORK_EMAIL_FROM="ANYwork <no-reply@your-domain.com>"

# OAuth provider credentials are configured in Supabase Auth.
# Never expose provider secrets as VITE_ variables.
